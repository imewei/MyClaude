---
name: commit
description: Retired — use commit-commands:commit or commit-commands:commit-push-pr
allowed-tools: Bash(git:*)
---

> **Retired.** Superseded by the `commit-commands` plugin (installed via `claude-plugins-official`).
>
> - Commit only → `/commit-commands:commit`
> - Commit + push + open PR → `/commit-commands:commit-push-pr`
> - Clean gone branches → `/commit-commands:clean_gone`
